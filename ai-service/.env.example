# Points at the SAME PostgreSQL database the Node backend uses,
# since this service reads (never writes) the products / stock_movements tables.
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/stock_alert_db

# CORS
CLIENT_URL=http://localhost:3000
NODE_API_URL=http://localhost:5000

PORT=8000
