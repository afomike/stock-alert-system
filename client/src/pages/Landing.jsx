import { Link, Navigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';

const features = [['Live visibility', 'See stock levels, inventory value, and movement in one focused workspace.'], ['Smart alerts', 'Catch low stock before it becomes a missed sale or an urgent supplier call.'], ['Clear decisions', 'Turn everyday inventory activity into reports your team can act on.']];

export default function Landing() {
  const { user, loading } = useAuth();
  if (!loading && user) return <Navigate to="/dashboard" replace />;
  return <div className="landing-page min-h-screen overflow-x-hidden bg-paper text-ink"><header className="landing-nav"><Link to="/" className="brand-mark" aria-label="StockWatch home"><span className="brand-symbol" aria-hidden="true"><i></i><i></i><i></i></span><span>StockWatch</span></Link><nav className="hidden md:flex items-center gap-8 text-sm font-medium text-ink/65"><a href="#features">Features</a><a href="#workflow">How it works</a></nav><Link to="/login" className="landing-login">Sign in <span aria-hidden="true">&rarr;</span></Link></header><main><section className="landing-hero"><div className="landing-copy"><p className="eyebrow"><span></span> INVENTORY, WITHOUT THE GUESSWORK</p><h1>Know what you have.<br /><em>Before it runs out.</em></h1><p className="hero-lede">StockWatch brings your products, suppliers, and stock alerts into a calm, clear command center.</p><div className="hero-actions"><Link to="/login" className="hero-primary">Get started <span aria-hidden="true">&rarr;</span></Link><a href="#features" className="hero-secondary">Explore features <span aria-hidden="true">&darr;</span></a></div><p className="hero-note"><span className="status-dot"></span> Built for teams that move stock every day</p></div><div className="dashboard-preview" aria-label="A preview of the StockWatch dashboard"><div className="preview-topbar"><div className="preview-logo"><span className="brand-symbol small"><i></i><i></i><i></i></span> StockWatch</div><div className="preview-avatar">A</div></div><div className="preview-body"><aside className="preview-sidebar"><span className="preview-nav active"></span><span className="preview-nav"></span><span className="preview-nav"></span><span className="preview-nav"></span></aside><div className="preview-content"><div className="preview-heading"><div><b>Inventory overview</b><small>Wednesday, August 17</small></div><span className="preview-add">+ Add product</span></div><div className="preview-metrics"><Metric label="Products" value="248" tone="blue" /><Metric label="In stock" value="18,425" tone="mint" /><Metric label="Low stock" value="12" tone="coral" /></div><div className="preview-chart"><div className="chart-label">Stock movement <small>Last 14 days</small></div><div className="chart-bars">{[35, 53, 42, 68, 57, 78, 63, 88, 70, 98, 84, 108].map((height, index) => <span key={index} style={{ height }} />)}</div></div><div className="preview-list"><div><b>Items needing attention</b><small>12 low stock items</small></div><span className="preview-pill">Review</span></div></div></div></div></section><section id="features" className="landing-features"><div className="feature-intro"><p className="eyebrow"><span></span> THE ESSENTIALS, DONE WELL</p><h2>Inventory management<br />that feels <em>effortless.</em></h2></div><div className="feature-grid">{features.map(([title, text], index) => <article key={title} className="feature-item"><span className={`feature-icon icon-${index + 1}`}>0{index + 1}</span><h3>{title}</h3><p>{text}</p></article>)}</div></section><section id="workflow" className="landing-banner"><p>Every stock decision starts with a clear picture.</p><Link to="/login">Open StockWatch <span aria-hidden="true">&rarr;</span></Link></section></main></div>;
}
function Metric({ label, value, tone }) { return <div className={`preview-metric ${tone}`}><small>{label}</small><b>{value}</b></div>; }
