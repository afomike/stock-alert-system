# Server
PORT=5000
NODE_ENV=development

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=stock_alert_db
DB_USER=postgres
DB_PASSWORD=Password
# Auth
JWT_SECRET=JWT_SECRET_KEY
JWT_EXPIRES_IN=7d

# Notifications (optional - stubbed by default)
EMAIL_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=afoteresearch@gmail.com
SMTP_PASS=xwnn lope wkbw ocf

# Python AI forecasting service (see ai-service/) — used by the
# /api/inventory/ai-* proxy routes for richer predictions
AI_SERVICE_URL=http://localhost:8000

# CORS
CLIENT_URL=http://localhost:3000
